<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.salya.savorcapstone">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SavorCapstone"
        tools:targetApi="31">

        <activity
            android:name=".view.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <activity
            android:name=".view.ViewActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <activity
            android:name=".view.InputOptionActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <activity
            android:name=".view.InputFromGalleryActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <activity
            android:name=".view.CameraActivity"
            android:exported="true"
            android:theme="@style/Theme.SavorCapstone">
        </activity>

        <!-- Other activities -->

    </application>

</manifest>
